import { PageHeader } from "@/components/page-header";
import { StatusBadge } from "@/components/status-badge";
import { getSettlements } from "@/lib/api";
import { formatDate, formatMoney } from "@/lib/format";

export default async function SettlementsPage() { const rows = await getSettlements(); return <><PageHeader eyebrow="PAYOUTS" title="Settlements" description="Compare expected and actual settlement amounts, fees, and adjustments."/><div className="mt-6 grid gap-4">{rows.map(s=><article className={`rounded-xl border bg-white p-5 shadow-card ${s.difference !== 0 ? "border-amber-300" : "border-slate-200"}`} key={s.id}><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-semibold">{s.external_settlement_id}</p><p className="mt-1 text-xs text-slate-500">{formatDate(s.settled_at)}</p></div><StatusBadge value={s.status}/></div><div className="mt-5 grid gap-4 sm:grid-cols-5">{[["Expected",s.expected_amount],["Actual",s.actual_amount],["Difference",s.difference],["Fees",s.fees],["Adjustments",s.adjustments]].map(([label,value])=><div key={String(label)}><p className="text-xs text-slate-500">{label}</p><p className={`mt-1 font-semibold ${label === "Difference" && value !== 0 ? "text-amber-700" : ""}`}>{formatMoney(Number(value))}</p></div>)}</div>{s.difference !== 0 && <p className="mt-4 rounded-lg bg-amber-50 px-3 py-2 text-sm text-amber-800">Discrepancy requires review: actual settlement differs from expected.</p>}</article>)}</div></>; }
